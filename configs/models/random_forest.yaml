model:
  name: "random_forest"
  family: "tree_ensemble"
  type: "classifier"
  explainer_type: "tree_shap"

hyperparameters:
  n_estimators: 200
  max_depth: 8
  min_samples_split: 5
  min_samples_leaf: 2
  random_state: 42
  n_jobs: -1

training:
  verbose: false
